port: 3000
idle: 30m
logLevel: info
startTimeout: 30s
stopTimeout: 10s

apps:
  api:
    pwd: /path/to/api
    build: go build ./...
    launch: ./api
    path: /service/api
    port: 1980
    idle: 5m
    includePrefix: false

  docs:
    pwd: /path/to/docs
    launch: npm run dev
    host: docs.localhost
    port: 1988
    idle: 5m

  postgres:
    pwd: /path/to/project
    launch: docker compose up postgres
    stop: docker compose stop postgres
    protocol: tcp
    listenPort: 15432
    port: 5432
    idle: 30m
